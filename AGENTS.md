# WARP VPS Manager 项目协作

本项目提供Google精准分流、全局WARP及WireGuard/Socks5管理。

## 核心约束

- Google规则只取官方`goog.json - cloud.json`的完整集合差，排除Cloud客户网段；不以固定数量或非官方列表代替集合核对。
- WireGuard与Socks5、精准与全局模式的协议边界以[README](README.md)为准。Socks5不承载UDP或IPv6；UDP探测不等于WireGuard已握手，不能把原生出口当作WARP结果。
- `warp-vps-rules-update.timer`的systemd启用状态是自动更新开关权威，不增加镜像字段。规则更新与程序更新职责分开，程序更新保留当前规则。
- 只管理项目拥有的路由、网卡、nftables对象和文件；共享依赖及其他项目资源不推定为本项目所有。系统路径按实际包管理器和网络能力选择。
- 解锁判断使用实际地区及正负页面特征，不复制第三方Cookie或用普通HTTP成功代替可用性；保留现有“无法确认”等正常结果。
- 维护现有Bash/ShellCheck、回归和规则生成检查，实机结论限定到实际系统、架构与模式。安装/更新只选择明确运行文件，不复制仓库根、cowork或localwork。

## 协作与交付

- 当前请求决定范围和输出位置，已有授权在本次任务内持续。查询、提取和回复草稿按请求交付，不自动生成报告或落盘记录。
- 基于当前实现与适用契约做最小修改，复用已有能力，不臆测接口，不增加未经要求的依赖或状态；保留现有合理容错。
- 区分只读、原样复用、无操作与真实写入。写入门禁只保护相应动作，不阻断合法读取或无操作。
- 验证匹配实际改动，不削弱检查；区分本地、模拟、测试环境与生产证据。只有实际修改仓库文件时，才审查diff、工作区、分支和远端，并按项目流程提交推送，不夹带无关改动。
- 本机文件需删除时使用系统安全回收，保留登录态、凭据、运行数据、未核实材料和唯一交付物。磁盘空间评估与可重建缓存整理仅由独立明确的清理任务处理，普通任务不附带磁盘治理。

## 资料入口

按当前任务选择[背景](cowork/background.md)、[项目规则](cowork/rules.md)、[待办](cowork/todo.md)、[技术文档](cowork/docs/README.md)和[错误记录](cowork/error/README.md)。本仓库Git中的规则与cowork是团队共享依据；根AGENTS保留核心约束，详细合同和操作在索引指向的唯一位置维护。

`cowork/`保持background、rules、todo三个Markdown文件及docs、error两个目录。`localwork/`仅保存个人本机备注与证据索引，不覆盖团队规则，不进入Git或发布产物；没有本机资料时保持空目录。

## Outline长期知识接入

- 团队知识库入口为 [Outline](https://internal.novixlink.com)，MCP endpoint 为 `https://internal.novixlink.com/mcp`。优先使用本机已配置的相关 MCP，以个人 OAuth 或已获授权的专用 Agent 身份接入；凭据、token 与认证 URL 不写入仓库。
- 按当前任务需要检索相关长期知识，不要求每次全库阅读。项目源码、合同和工程状态仍由本仓库既有权威维护，实时业务事实由对应系统回读；Outline 不建立第二套工程状态。
- 已确认且可共享的长期知识发生变化时，先更新既有权威，再按已获授权的知识库写入范围同步对应 Outline 页面并回读，不为范围内的同步重复申请授权。普通查询、一次性草稿和无实质变化不新建记录；秘密、会话、私人材料及客户原始记录不进入 Outline。
